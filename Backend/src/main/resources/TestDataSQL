-- =====================================================================
-- ToDoApp — Compleet testscript voor pgAdmin
-- Dekt: users, project, project_member, sprint, task_list, task
-- Wachtwoord voor ALLE users hieronder: password123
-- =====================================================================

-- Alles leegmaken en ID-tellers resetten, in de juiste volgorde
-- (kinderen eerst, dan ouders, vanwege foreign keys)
TRUNCATE TABLE task, task_list, sprint, project_member, project, users
RESTART IDENTITY CASCADE;

-- ---------------------------------------------------------------------
-- USERS
-- id 1 = Stefan (Admin), id 2 = Jan, id 3 = Piet, id 4 = Emma
-- ---------------------------------------------------------------------
INSERT INTO users (first_name, last_name, email, password, is_admin) VALUES
('Stefan', 'Kiers',      'stefan@test.nl', '$2b$12$y3.a6WOl7KHyFKMOMx2acen9CVmOBBggVnLxOrook9.zs6xrVPIOC', true),
('Jan',    'Jansen',     'jan@test.nl',    '$2b$12$y3.a6WOl7KHyFKMOMx2acen9CVmOBBggVnLxOrook9.zs6xrVPIOC', false),
('Piet',   'Pietersen',  'piet@test.nl',   '$2b$12$y3.a6WOl7KHyFKMOMx2acen9CVmOBBggVnLxOrook9.zs6xrVPIOC', false),
('Emma',   'de Vries',   'emma@test.nl',   '$2b$12$y3.a6WOl7KHyFKMOMx2acen9CVmOBBggVnLxOrook9.zs6xrVPIOC', false);

-- ---------------------------------------------------------------------
-- PROJECT
-- id 1 = Website Redesign, id 2 = Interne Tool
-- ---------------------------------------------------------------------
INSERT INTO project (title, deadline) VALUES
('Website Redesign', '2026-12-31'),
('Interne Tool',      '2027-03-31');

-- ---------------------------------------------------------------------
-- PROJECT_MEMBER — koppelt users aan projects met een rol
-- Jan = Scrum Master op project 1, Piet = Developer op project 1,
-- Emma = Client op project 1 (kijkt alleen mee)
-- ---------------------------------------------------------------------
INSERT INTO project_member (project_id, user_id, role) VALUES
(1, 2, 'SCRUM_MASTER'),
(1, 3, 'DEVELOPER'),
(1, 4, 'CLIENT');

-- ---------------------------------------------------------------------
-- SPRINT — hangt aan een project
-- id 1 = Sprint 1 (actief), id 2 = Sprint 2 (gepland)
-- ---------------------------------------------------------------------
INSERT INTO sprint (title, description, start_date, end_date, sprint_status, project_id) VALUES
('Sprint 1', 'Eerste sprint: basisopzet', '2026-09-01', '2026-09-14', 'ACTIVE',  1),
('Sprint 2', 'Tweede sprint: uitbreiding', '2026-09-15', '2026-09-28', 'PLANNED', 1);

-- ---------------------------------------------------------------------
-- TASK_LIST — hangt aan een user (los van Project/Sprint, dit is de
-- oorspronkelijke, kale to-do-lijst-structuur van Fase 0)
-- ---------------------------------------------------------------------
INSERT INTO task_list (title, user_id) VALUES
('Werk', 1),
('Studie', 1),
('Persoonlijk', 2);

-- ---------------------------------------------------------------------
-- TASK — hangt aan een task_list en een user
-- ---------------------------------------------------------------------
INSERT INTO task (title, content, task_list_id, status, user_id, migration_count, created_at) VALUES
('Backend afmaken',   'Spring Boot API compleet maken', 1, 'IN_PROGRESS', 1, 0, CURRENT_DATE),
('Frontend bouwen',   'React components maken',         1, 'CREATED',     1, 0, CURRENT_DATE),
('Sprint 1 afronden', 'Alle taken van sprint 1 checken', 2, 'CREATED',     1, 0, CURRENT_DATE);

-- =====================================================================
-- Verificatie — draai deze losse SELECT's om te checken of alles klopt
-- =====================================================================
-- SELECT * FROM users;
-- SELECT * FROM project;
-- SELECT pm.id, p.title AS project, u.first_name, pm.role
--   FROM project_member pm
--   JOIN project p ON p.id = pm.project_id
--   JOIN users u ON u.id = pm.user_id;
-- SELECT * FROM sprint;
-- SELECT * FROM task_list;
-- SELECT * FROM task;
