Api endpoints : 

GET /tasklist : 
GET/taskList/{id}
GET tasks/tasksList/{ id}
GET /users
GET /users/{id}
POST /taskList
POST / users

Af hebben voor aankomende dinsdag (16 - 12 - '25)
- inloggen met afgerond zijn
- alleen mijn takenlijsten zien (niet hardcoded maar als object van gebruiker zelf)
- taak aanmaken die in lijst naar voren komt
- als ik uitlog moet die data afgeschermd worden
- columns moeten cards worden (kunnen alsnog in column), cards koppelen aan specifieke tasklists
- user ophalen en koppelen aan tasklist